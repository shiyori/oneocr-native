# Synthetic 1280×720 fixtures

Generated with macOS CoreText using the included `render.swift`. `annotations.json` contains source text (ground truth), dimensions and font sizes. Mixed Chinese/Japanese/Korean/English/digits include punctuation and spaces; evaluation must retain these differences. These are synthetic regression inputs, not a production accuracy benchmark.
